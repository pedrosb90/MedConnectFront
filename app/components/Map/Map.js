import "leaflet/dist/leaflet.css"
import {MapContainer, TileLayer} from "react-leaflet"


function Map() {
    return (
        <MapContainer>
            <TileLayer></TileLayer>
        </MapContainer>
    )
    
}